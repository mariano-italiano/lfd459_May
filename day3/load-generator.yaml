apiVersion: v1
kind: Pod
metadata:
  name: load-generator
spec:
  containers:
    - name: load-pod
      image: fortio/fortio
      args: ["load", "hpa-deployment.default.svc.cluster.local"]
  restartPolicy: Never
